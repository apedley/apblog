.page-header
  %h1
    = @post.title
    %small= @post.subtitle

%p.tags
  - @post.tags.map(&:name).each do |t|
    = link_to t, tag_path(t)
%p.author
  = link_to @post.user.name, @post.user
.body= raw @post.body_html

- if current_user
  .links
    = link_to 'Edit', edit_post_path(@post)
    \|
    = link_to 'Back', posts_path
