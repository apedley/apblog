.tag_list
  %h3 Tags
  - Tag.used.take(12).each do |t|
    %p.tag= link_to "#{t.name.capitalize} (#{t.taggings.count})", tag_path(t.name) 
%hr
.tag_cloud
  %ul.tag_cloud
    - tag_cloud(Post.tag_counts, %w{s m l}) do |tag, css_class|
      %li{ 'class' => css_class }
        = link_to tag.name, tag_path(tag.name), class:css_class


